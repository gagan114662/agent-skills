.DS_Store
node_modules/
dist/
dist-ssr/
build/
*.log
.env
.env.*
!.env.example
coverage/

# Vercel CLI link/build artifacts (#108)
.vercel/

# conductor workspace scratch
.context/
.conductor/
.gstack/

# Lavish Editor (#344): local HTML-artifact review session state
.lavish-axi/

# Reload (#58): active file-backed config + provisioned per-session workspaces (examples are tracked)
.reload/settings.toml
.reload/managed.toml
.reload/workspaces/

# Reload (#99): local DR dump store (dryrun-by-default object store + validation-drill scratch)
.dr-backups/
.vercel

# Reload (#155): offline eval-run report artifact (regenerated by scripts/run-evals.ts each CI run)
platform/.eval-report.json
